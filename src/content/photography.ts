/**
 * Maps each PhotoFrame caption to its real photography asset in /public/images.
 * A caption with no entry here still renders as a placeholder frame.
 */
export const photography: Record<string, string> = {
  // Populated as real photography is generated and added to /public/images.
  // A caption with no entry here still renders as a placeholder frame.
};
